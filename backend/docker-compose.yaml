version: "3.9"

services:
  backend:
    build: .
    container_name: world_builder_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - LANGCHAIN_TRACING_V2=false
      - PYTHONPATH=/app
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
